<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Betaling in Behandeling</title>
</head>
<body>
    <h1>Betaling in Behandeling</h1>
    <p>Uw betaling is in behandeling. Het kan enkele ogenblikken duren voordat we een bevestiging ontvangen.</p>

    <script>
    function checkPaymentStatus() {
        const paymentId = sessionStorage.getItem('currentPaymentId');
        if (!paymentId) {
            console.error('No paymentId available');
            return;
        }

        fetch(`/payments/check-payment-status/${paymentId}`)
            .then(response => response.json())
            .then(data => {
                console.log('Payment status:', data);
                if (data.redirectUrl) {
                    window.location.href = data.redirectUrl;
                } else {
                    setTimeout(checkPaymentStatus, 5000);
                }
            })
            .catch(error => {
                console.error('Error checking payment status:', error);
                setTimeout(checkPaymentStatus, 10000);
            });
    }

    document.addEventListener('DOMContentLoaded', checkPaymentStatus);
    </script>
</body>
</html>